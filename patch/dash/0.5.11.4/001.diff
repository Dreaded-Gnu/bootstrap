diff --git src/jobs.c src/jobs.c
index f30313b..83af38d 100644
--- src/jobs.c
+++ src/jobs.c
@@ -1179,7 +1179,7 @@ waitproc(int block, int *status)
 	do {
 		gotsigchld = 0;
 		do
-			err = wait3(status, flags, NULL);
+			err = waitpid( -1, status, flags );
 		while (err < 0 && errno == EINTR);
 
 		if (err || (err = -!block))
diff --git src/mksignames.c src/mksignames.c
index a832eab..a125434 100644
--- src/mksignames.c
+++ src/mksignames.c
@@ -28,6 +28,11 @@
 #  define NSIG 64
 #endif
 
+#if NSIG != 32
+#undef NSIG
+#define NSIG 32
+#endif
+
 /*
  * Special traps:
  *	EXIT == 0

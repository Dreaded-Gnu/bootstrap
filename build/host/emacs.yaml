
- name: host-emacs
  source: emacs@28.1
  dependency:
    - host-patch
  prepare:
    - '{SOURCE_DIR}/configure --prefix={PREFIX}'
  build:
    - 'make -j{CPU_COUNT}'
  install:
    - 'make install'
